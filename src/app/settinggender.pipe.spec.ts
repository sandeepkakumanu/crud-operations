import { SettinggenderPipe } from './settinggender.pipe';

describe('SettinggenderPipe', () => {
  it('create an instance', () => {
    const pipe = new SettinggenderPipe();
    expect(pipe).toBeTruthy();
  });
});
